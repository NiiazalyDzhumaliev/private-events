<p><strong><%=@user.username%></strong></p>

<h1>User has created:</h1>

<%@user.events.each do |event|%>
  <span><%=event.desc%></span>
  <span><%= link_to 'Invite users', new_event_event_attendances_path(event) %></span>
  <br/>
<%end%>


<h1>Previous Attended Events</h1>
<p>
    <strong><span>Description<span></strong>
    <strong><span>Date<span></strong>
  </p>
  <%= render partial: 'event', collection:  @user.attended_events.previous_events %>
 
  <h1>Upcoming Attending Events</h1>
  <p>    
    <strong><span>Description<span></strong>
    <strong><span>Date<span></strong>
  </p>
  <%= render partial: 'event', collection:  @user.attended_events.upcoming_events %>
  


